/**
 * Semantic palette aligned with the design brief.
 * All values are Tailwind v4 color scale roots—use in classes as
 * e.g. bg-{primary}-900, text-{secondary}-500, border-{accentRose}-300.
 * Light/dark behavior comes from dark: variants, not separate hex maps.
 */
export const primary = "slate";
export const secondary = "sky";
export const neutral = "neutral";
export const accentRose = "rose";
export const accentWarm = "amber";
export const accentSuccess = "emerald";

export const semanticColors = {
  primary,
  secondary,
  neutral,
  accentRose,
  accentWarm,
  accentSuccess,
};

export default semanticColors;
